/**
 * @healing/db — Postgres 스키마와 접속.
 *
 * **P0 에서는 자리만 잡습니다.** 내용은 P2 에서 채웁니다.
 *   1. Drizzle 스키마 — docs/ARCHITECTURE.md 4.2절 SQL 그대로
 *   2. 마이그레이션 생성·적용
 *   3. pg-boss 큐 연결
 *
 * users 테이블은 만들지 않습니다. 1차에 회원 개념이 없습니다.
 * 나중에 쓸 거라고 nullable 컬럼을 미리 만들지도 않습니다.
 * docs/ARCHITECTURE.md 4.2 · docs/TASKS.md P2
 */

export {};
