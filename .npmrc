# 워크스페이스 패키지는 항상 링크로 해결한다.
link-workspace-packages=true
# 엔진 요구(Node 22)를 어기면 설치를 멈춘다.
engine-strict=true
# pdfjs-dist 는 4.10.38 로 고정한다. docs/OMR.md 5.7 · ARCHITECTURE.md 2.1
# 자동 올림을 막기 위해 캐럿 범위를 쓰지 않는다.
save-exact=false
